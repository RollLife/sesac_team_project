apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10 # 10초마다 파일 변경 감지
    options:
      path: /var/lib/grafana/dashboards  # (중요) 컨테이너 내부에서 JSON 파일을 찾을 위치