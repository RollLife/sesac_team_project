# ===========================================
# 데이터베이스 설정
# ===========================================
# 개발 환경: local PostgreSQL
# 운영 환경: 실제 DB 호스트/포트로 변경

DB_TYPE=local
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=sesac_db
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# ===========================================
# Kafka 설정
# ===========================================
# 개발 환경: Docker 내부 네트워크 주소
# 운영 환경: 실제 Kafka 브로커 주소로 변경

KAFKA_BOOTSTRAP_SERVERS=kafka1:29092,kafka2:29093,kafka3:29094
KAFKA_ENABLED=true

# ===========================================
# Kafka 클러스터 설정
# ===========================================
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk

# ===========================================
# 사용 방법
# ===========================================
# 1. 이 파일을 복사하여 .env 파일 생성:
#    cp .env.example .env
#
# 2. .env 파일의 값을 환경에 맞게 수정
#
# 3. docker-compose 실행:
#    docker-compose up -d
#
# 주의: .env 파일은 .gitignore에 추가하여 버전 관리에서 제외하세요
